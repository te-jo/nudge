# Postgres connection string. For local dev, create a free project at
# https://neon.tech and paste the connection string from the dashboard.
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

PORT=3000
