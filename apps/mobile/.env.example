# Optional override for the API base URL. In dev this is normally
# auto-derived from the Expo packager's host IP (see src/lib/api.ts) so a
# physical device on the same network can reach a laptop running the API.
# Set this explicitly for staging/prod builds, or if auto-detection fails.
# EXPO_PUBLIC_API_URL=http://192.168.1.23:3000
